# CSRF protection configuration
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        # Utiliser stateful pour le login (nécessite une session)
        # Cela résout les problèmes avec Railway et les proxies
        stateless_token_ids: []
